'use strict';


describe(' Modal settings expects ',function(){

   beforeEach(module('notebookWebApp'));

   describe(' When cross-data settings was selected ',function(){

    var controller,scope,fakeModal,http,baseUrlServ;

      /* beforeEach(inject(function($controller,$rootScope,_baseUrlSrv_){
          fakeModal = sinon.spy();
          http = sinon.spy();
          scope = $rootScope.$new();
          baseUrlServ = sinon.spy();

          controller = $controller('ModalSettingsCtrl', {
              $scope: scope,
              $modalInstance :fakeModal
          });
       }));

       it(' and is first time ',function(){
            controller.getCrossdataInterpreterSettings();
            expect(scope.showCrossdataProperties).to.be.equal(true);
       });*/

   });

});

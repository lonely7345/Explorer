#!/bin/bash

# export JAVA_HOME=
# export MASTER=spark://host:7077  # spark master
# export KAFKA=localhost:9092
# export ZOOKEEPER=localhost:2181
# export NOTEBOOK_JAVA_OPTS      # additional jvm options. for example, export ZEPPELIN_JAVA_OPTS="-Dspark.executor.memory=8g -Dspark.cores.max=16"
# export NOTEBOOK_MEM            # zeppelin jvm mem options Defualt -Xmx1024m -XX:MaxPermSize=512m

# export NOTEBOOK_CONF_DIR       # Alternate zeppelin conf dir. Default is ${HBASE_HOME}/conf.
# export NOTEBOOK_LOG_DIR        # Where log files are stored.  PWD by default.
# export NOTEBOOK_PID_DIR        # The pid files are stored. /tmp by default.
# export NOTEBOOK_NOTEBOOK_DIR   # Where notebook saved
# export NOTEBOOK_IDENT_STRING   # A string representing this instance of zeppelin. $USER by default
# export NOTEBOOK_NICENESS       # The scheduling priority for daemons. Defaults to 0.
# export NOTEBOOK_SPARK_USEHIVECONTEXT    # Use HiveContext instead of SQLContext if set true. true by default.
# export NOTEBOOK_JAVA_OPTS="-Dspark.executor.uri=/path/to/spark-*.tgz" or SPARK_HOME="/path/to/spark_home"
# export MESOS_NATIVE_LIBRARY=/path/to/libmesos.so # When connecting to a mesos master, this should be set
